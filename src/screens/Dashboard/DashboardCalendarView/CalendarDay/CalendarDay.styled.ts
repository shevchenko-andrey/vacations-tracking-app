import { styled, Box } from '@mui/material';

export const CalendarCell = styled(Box)(() => ({
  cursor: 'pointer',
  padding: '10px',
  color: '#fff',
  minHeight: '200px',
  position: 'relative',
}));
